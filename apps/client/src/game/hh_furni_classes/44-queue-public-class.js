export default class {
  pState;
  pAnimFrame;
  pFrameCounter;
  pAnimStartTime;
  pAnimate;

  prepare(tdata) {
    this.pCorrectLocZ = 0;
    return 1;
  }

  updateStuffdata(tValue) {
    return 1;
  }

  setAnimation(tValue) {
    return 1;
  }

  update() {
  }
}
