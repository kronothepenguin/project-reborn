## 1. Put Helper Implementations

- [x] 1.1 Implement `putInto()` function in `apps/client/src/director/syntax/put-into.js` per `specs/director-syntax-put/put-into.md`
- [x] 1.2 Implement `putBefore()` function in `apps/client/src/director/syntax/put-before.js` per `specs/director-syntax-put/put-before.md`
- [x] 1.3 Implement `putAfter()` function in `apps/client/src/director/syntax/put-after.js` per `specs/director-syntax-put/put-after.md`

## 2. Put Helper Tests

- [x] 2.1 Write tests for `putInto()` in `apps/client/src/director/syntax/__tests__/put-into.test.js`
- [x] 2.2 Write tests for `putBefore()` in `apps/client/src/director/syntax/__tests__/put-before.test.js`
- [x] 2.3 Write tests for `putAfter()` in `apps/client/src/director/syntax/__tests__/put-after.test.js`

## 3. Export and Integration

- [x] 3.1 Export all put helpers from `apps/client/src/director/syntax/index.js`
- [x] 3.2 Verify all put helpers are accessible via `import { putInto, putBefore, putAfter } from "../../director/syntax"`
- [x] 3.3 Run all tests to ensure implementation matches Director MX 2004 behavior
