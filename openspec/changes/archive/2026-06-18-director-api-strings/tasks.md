## 1. String Function Implementations

- [x] 1.1 Implement `chars()` function in `apps/client/src/director/api/chars.js` per `specs/director-api-strings/chars.md`
- [x] 1.2 Implement `length()` function in `apps/client/src/director/api/length.js` per `specs/director-api-strings/length.md`
- [x] 1.3 Implement `offset()` function in `apps/client/src/director/api/offset.js` per `specs/director-api-strings/offset.md`

## 2. String Function Tests

- [x] 2.1 Write tests for `chars()` in `apps/client/src/director/api/__tests__/chars.test.js`
- [x] 2.2 Write tests for `length()` in `apps/client/src/director/api/__tests__/length.test.js`
- [x] 2.3 Write tests for `offset()` in `apps/client/src/director/api/__tests__/offset.test.js`

## 3. Export and Integration

- [x] 3.1 Export all string functions from `apps/client/src/director/api/index.js`
- [x] 3.2 Verify all string functions are accessible via `import { chars, length, offset } from "../../director/api"`
- [x] 3.3 Run all tests to ensure implementation matches Director MX 2004 behavior
