## 1. Bitwise Function Implementations

- [x] 1.1 Implement `bitAnd()` function in `apps/client/src/director/api/bitAnd.js` per `specs/director-api-bitwise/bitAnd.md`
- [x] 1.2 Implement `bitNot()` function in `apps/client/src/director/api/bitNot.js` per `specs/director-api-bitwise/bitNot.md`
- [x] 1.3 Implement `bitOr()` function in `apps/client/src/director/api/bitOr.js` per `specs/director-api-bitwise/bitOr.md`
- [x] 1.4 Implement `bitXor()` function in `apps/client/src/director/api/bitXor.js` per `specs/director-api-bitwise/bitXor.md`

## 2. Bitwise Function Tests

- [x] 2.1 Write tests for `bitAnd()` in `apps/client/src/director/api/__tests__/bitAnd.test.js`
- [x] 2.2 Write tests for `bitNot()` in `apps/client/src/director/api/__tests__/bitNot.test.js`
- [x] 2.3 Write tests for `bitOr()` in `apps/client/src/director/api/__tests__/bitOr.test.js`
- [x] 2.4 Write tests for `bitXor()` in `apps/client/src/director/api/__tests__/bitXor.test.js`

## 3. Export and Integration

- [x] 3.1 Export all bitwise functions from `apps/client/src/director/api/index.js`
- [x] 3.2 Verify all bitwise functions are accessible via `import { bitAnd, bitOr, bitXor, bitNot } from "../../director/api"`
- [x] 3.3 Run all tests to ensure implementation matches Director MX 2004 behavior
