## 1. List Function Implementations

- [x] 1.1 Implement `list()` function in `apps/client/src/director/api/list.js` per `specs/director-api-lists/list.md`
- [x] 1.2 Implement `propList()` function in `apps/client/src/director/api/propList.js` per `specs/director-api-lists/propList.md`
- [x] 1.3 Implement `count()` function in `apps/client/src/director/api/count.js` per `specs/director-api-lists/count.md`
- [x] 1.4 Implement `duplicate()` function in `apps/client/src/director/api/duplicate.js` per `specs/director-api-lists/duplicate.md`
- [x] 1.5 Implement `makeSubList()` function in `apps/client/src/director/api/makeSubList.js` per `specs/director-api-lists/makeSubList.md`
- [x] 1.6 Implement `union()` function in `apps/client/src/director/api/union.js` per `specs/director-api-lists/union.md`

## 2. List Function Tests

- [x] 2.1 Write tests for `list()` in `apps/client/src/director/api/__tests__/list.test.js`
- [x] 2.2 Write tests for `propList()` in `apps/client/src/director/api/__tests__/propList.test.js`
- [x] 2.3 Write tests for `count()` in `apps/client/src/director/api/__tests__/count.test.js`
- [x] 2.4 Write tests for `duplicate()` in `apps/client/src/director/api/__tests__/duplicate.test.js`
- [x] 2.5 Write tests for `makeSubList()` in `apps/client/src/director/api/__tests__/makeSubList.test.js`
- [x] 2.6 Write tests for `union()` in `apps/client/src/director/api/__tests__/union.test.js`

## 3. Export and Integration

- [x] 3.1 Export all list functions from `apps/client/src/director/api/index.js`
- [x] 3.2 Verify all list functions are accessible via `import { list, propList, count } from "../../director/api"`
- [x] 3.3 Run all tests to ensure implementation matches Director MX 2004 behavior
