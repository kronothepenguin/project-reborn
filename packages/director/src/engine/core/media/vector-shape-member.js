import { MemberObject } from "../member.js";

/**
 * Vector Shape cast member (Chapter 6: Media Types — "Vector Shape").
 *
 * Represents a vector shape cast member.
 *
 * Method summary: `addVertex()`, `deleteVertex()`, `moveVertex()`, `moveVertexHandle()`,
 * `newCurve()`, `showProps()`.
 * Property summary: `antiAlias`, `backgroundColor`, `broadcastProps`, `centerRegPoint`, `closed`,
 * `curve`, `defaultRect`, `defaultRectMode`, `endColor`, `fillColor`, `fillCycles`,
 * `fillDirection`, `fillMode`, `fillOffset`, `fillScale`, `flashRect`, `gradientType`,
 * `imageEnabled`, `originH`, `originMode`, `originPoint`, `originV`, `regPointVertex`, `scale`,
 * `scaleMode`, `strokeColor`, `strokeWidth`, `vertex`, `vertexList`, `viewH`, `viewPoint`,
 * `viewScale`, `viewV`.
 *
 * Excluded from v1 full implementation per FR-012 (stub only).
 */
export class VectorShapeMember extends MemberObject {}