import { MemberObject } from "../member.js";

/**
 * Windows Media cast member (Chapter 6: Media Types — "Windows Media").
 *
 * Represents a Windows Media cast member.
 *
 * Method summary: `pause()`, `play()`, `playFromToTime()`, `rewind()`, `stop()`.
 * Property summary: `audio`, `directToStage`, `duration`, `height`, `loop`, `mediaStatus`,
 * `pausedAtStart`, `playRate`, `video`, `volume`, `width`.
 *
 * Excluded from v1 full implementation per FR-012 (stub only).
 */
export class WindowsMediaMember extends MemberObject {}