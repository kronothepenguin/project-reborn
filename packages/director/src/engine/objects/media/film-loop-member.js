import { MemberObject } from "../member.js";

/**
 * Film Loop cast member (Chapter 6: Media Types — "Film Loop").
 *
 * Represents a film loop cast member.
 *
 * Property summary: `media`, `regPoint` (both inherited from Member — no class-specific surface).
 * Excluded from v1 full implementation per FR-012 (stub only).
 */
export class FilmLoopMember extends MemberObject {}