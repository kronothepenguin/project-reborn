import { MemberObject } from "../member.js";

/**
 * Flash Component cast member (Chapter 6: Media Types — "Flash Component").
 *
 * Represents a Macromedia Flash component that is embedded in a cast member or sprite that
 * contains Flash content.
 *
 * Excluded from v1 full implementation per FR-012 (stub only). Flash components are created and
 * supported entirely by the Director development community; the v1 stub represents the cast-member
 * slot only.
 */
export class FlashComponentMember extends MemberObject {}